{#{% include '_partial/modal/_modal_content.html.twig' with {#}
{#    action: 'Approuver',#}
{#    classeAction: classeAction|default('success'),#}
{#} only %}#}

{% include '_partial/modal/_modal_confirmer.html.twig' with {
    messageConfirmation: "Voulez-vous vraiment approuver ?"|e,
    submit: true,
    action: 'Approuver',
    classeAction: classeAction|default('success'),
    urlFormulaire: urlFormulaire|default,
    urlMajContent: urlMajContent|default,
} only %}