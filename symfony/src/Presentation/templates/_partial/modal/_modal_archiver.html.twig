{% include '_partial/modal/_modal_confirmer.html.twig' with {
    messageConfirmation: "Voulez-vous vraiment archiver cet élément ?"|e,
    submit: true,
    action: 'Archiver',
    classeAction: classeAction|default('warning'),
    urlFormulaire: urlFormulaire|default,
    urlMajContent: urlMajContent|default,
} only %}